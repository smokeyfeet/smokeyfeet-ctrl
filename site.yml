---

- name: Smokey Feet
  hosts: web

  pre_tasks:
    - name: Ensure apt cache updated
      sudo: yes
      apt:
        cache_valid_time: 86400
        update_cache: yes

  roles:
    - role: base
      sudo: yes

    - role: postgres
      sudo: yes

    - role: smokeyfeet
